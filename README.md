# training-dev3
git with init


“First commit after cloning!!!”

``` ts
function abc(x, y) {
    if(!x) return 0;
    if(!y) return 1;
    console.error(x);
    console.error(y);
    return x + y;
}
```
